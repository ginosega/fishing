# Fishing Context

**Status:** Active v1 production; v2 requirements and technical design approved. Repository audit complete, application implementation not started. Reconciled September 8, 2026 (Pacific time). Restore actual main before acting; this checkpoint is not a substitute for current source.

## Authority and current v2 phase

Repository: `ginosega/fishing`. The approved future design is [Fishing_Companion_v2_Approved_Baseline.md](Fishing_Companion_v2_Approved_Baseline.md). Exact user answers remain in the Requirements Inventory and Design Review at decision commit `0bd773366130a302b0e80d5cd085a71731ff9e63`. All fourteen decisions are resolved. The user explicitly authorized the source/dependency/data audit and minimal technical contracts; both have been completed and recorded in [Source Audit](Fishing_Companion_v2_Source_Audit.md) and [Technical Contracts](Fishing_Companion_v2_Technical_Contracts.md). The latter includes a validated draft-2020-12 domain schema at `v2/contracts/schema.json`. No v2 build, source migration, preview or production cutover has occurred. No P2 direct Save, authentication service or offline sync is authorized.

V2 uses three small independent domain contracts with shared identity, Markdown, picture, path and validation conventions. Gear has ordinary independent rod/reel records; Catch retains only optional Species, Location and Lure/Bait relationships, text Size and Species-derived pictures. Gear/KB representative pictures use a local path and authored caption, not a media owner/provenance graph. Full-library offline reading and minimal Gear/KB handoffs are P1. Direct Save, integrated uploads, offline authoring and Catch browser authoring are P2. The approved baseline governs exact fields, folder rules, UI and migration. Existing v1 architecture remains authoritative for operating the live application until replaced.

## Verified source and production state

The approved source audit is pinned to main `1fa82a0cb653b8205bc8a45a1cf4608a616bf3b2`. Read-only audit run #2 / `34312003986` succeeded and produced a complete source snapshot and inventory. Excluding two audit-only files, the source has 247 tracked files. The successful production bundle from #312 / `34237232075`, main `4f2fe70f47da9cca3704722de87f7282bcc00f83`, was retrieved and inspected. Source Gear/Catch JSON is identical to the published data; KB non-picture fields are identical; all 101 authored Markdown files are byte-identical to their published copies. Twelve source tests, four dist tests and the final-bundle verifier passed locally. No independent live-browser acceptance is claimed.

The last fully documented application feature release remains PR60, final head `cd47dc9ce39660840de493346e7df9fda72a14e5`, CI #307 / `34191643311`, merge `ecd9f3d52ca8180ea4f48ec888e7574105deafda`, production #308 / `34191692935`. Subsequent direct-main edits and documentation are newer. The live URL is https://ginosega.github.io/fishing/. Run #311 failed because a content test expected a deleted Bonafide source filename; #312 succeeded. The earlier recovery record documents stale full-text assertions. The audit records confirmed failure classes and distinguishes unverified historical failures from source-supported risks.

Current source Gear schema4/66 uses dataVersion `2026-09-08-my-gear-v4-perception-joyride-1`; KB schema1/54 uses `2026-09-08-kb-v1-cranberry-lake-picture-1`; Catch schema2/5 uses `2026-09-04-catches-v2-external-notes-1`. These are current audited counts, not permanent migration expectations. Source records and current Git history override historical checkpoints.

V1 Gear facts live in `pwa/data/gear.seed.json` and `gear-store.js` IndexedDB stores; non-seed imports may persist separately. Optional Notes use `pwa/gear-content/`. KB uses `pwa/data/kb.seed.json` and `pwa/kb-content/`. Catch uses `pwa/data/catches.seed.json` and `pwa/catch-content/`. Media is spread across four registries, source assets and generated overlays. The existing build and workflow remain intact. See `pwa/README.md` for current v1 commands. Do not remove working v1 code or browser data merely because v2 has approved replacements.

## Source audit preservation findings

The source has 66 Gear records (three paired setups), 54 KB entries, five Catches, and 101 authored documents (42 Gear, 54 KB, five Catch). There are 36 repository content images totaling 20,378,972 bytes, plus the trusted application icon. The verified bundle has 65 published Gear media records and 88 image files. Twenty-five KB source/display records differ only in picture overlays; all authored internal references resolve. Complete path, byte, SHA-256, source/published media and component-migration inventories are retained in the audit evidence.

Fifty-one Gear image captures have no identical current repository source, and three standalone KB pictures use remote URLs. Six KB pictures reuse Gear assets and require no extra copies. The existing captured Gear bytes are preserved in the successful archived bundle. The active Tsuridamashii snap-swivels WebP fails full decoding; an unused Kokanee WebP is also invalid, while the active Kokanee PNG is valid. The Rapala Original Floating F-3 has no published picture and a second generic inline-spinner item has no representative-picture assignment. Existing images with valid decoding meet the approved size/dimension limits. The media task is FISH078. Default to user-supplied local replacements; adopting archived captures as local originals requires explicit approval. No automatic web acquisition.

Browser-only Gear data is not known to exist, but the repository cannot prove that all devices contain only seed-managed data. FISH079 requires a one-time read-only IndexedDB export/difference check before the old store is retired. Preserve any differences and do not clear browser data. The complete offline-library/browser acceptance remains a future implementation gate, not a result of the source audit.

## Rod/reel and Catch migration decisions

The current setup IDs are `setup-spinning`, `setup-baitcasting`, `setup-spincasting`. V2 creates six ordinary records using the exact mapping in the Source Audit and adds Spincasting rod as the sixth Type. Copy each component's current Manufacturer, Model, Specifications and Links exactly. Duplicate the complete original setup Notes into two byte-identical Markdown files, one for each component. The user will correct facts and Notes through Edit afterward. Preserve unaffected IDs and reconcile retired setup links explicitly. No invented rod model, ownership, picture or historical Catch attribution. With unchanged source counts, the expected Gear count is 69.

The five current Catches have valid known Species/Location/Lure associations, null setup/technique/picture fields, and original measurement objects. V2 text sizes are 13 in, 10 in, 12 in, 12 in and 12 in. Preserve all original values/units, dates, authored Notes and exact relationships; retain removed structured facts in migration evidence or Notes where appropriate. No session, trip, setup or technique relationship is reintroduced.

## Current equipment and media facts

Perception Joyride 10.0: Gear ID `perception-joyride-10-0`, category `accessories`, type `Accessories`, manufacturer Perception, model Joyride 10.0. Recorded specifications: Length `10'`, Height `15.25"`, Width `28.5"`, Weight `50 lb`, Color `Funkadelic`. The accepted product link and no-Notes state remain authoritative in source. Current source image `pwa/assets/gear-source/perception-joyride-10.png`, Git blob `d0f639c5c0309decd088f07e8cc65ee41b87ae74`; historical hashes remain in release history.

Dagger Axis 10.5: ID `dagger-axis-10-5`, confirmed Length `10' 6"`. Preserve current facts and no-Notes state. Current source image `pwa/assets/gear-source/dagger-axis-10-5.png`, Git blob `ba588a299f6d47521488c87dc613ec6cf46f65d6`. Bonafide RVR119 and its expanded authored Notes remain intact; current source image `pwa/assets/gear-source/bonafide-rvr119.png`, Git blob `af6f81cdc003e8a7fd3c9a7546d4a80ce400fe04`. These are current audit evidence, not permanent byte locks.

Cranberry Lake: ID `location-cranberry-lake-deception-pass`, original complete Markdown and picture `./assets/kb/entries/location-cranberry-lake-deception-pass.png`, alt/caption `Cranberry Lake`. Fishing Line, Walking Bait and Rods & Reels hero images and complete articles are preserved. Rods & Reels caption is `Baitcasting reel`; Fishing Line's explicit Sufix 832 association must not be inferred into unrelated ownership. Current source bytes prevail over earlier release hashes.

Documented v1 page copy: home/KB root `Fishing reference and catch log`; Gear Guides `Equipment, rig, and presentation reference`; Catch Log `Recorded catches`; Techniques `Strategy, conditions, and species reference` without a final period. V2 retains the approved visual language and exact updated UI requirements. Do not infer purchases or current equipment from historical research.

## Next implementation and release boundaries

The audit and design contracts are complete; the next milestone is the small representative vertical slice, followed by full migration, complete P1 UI/offline acceptance and the one-time separate preview/cutover. The selected approach is framework-free JS/ESM, small shared contracts, one explicit router, tested Markdown/sanitization, one deterministic build and versioned complete offline releases. Dependency candidates and semantic/authoring/offline contracts are documented; no production dependency or v2 runtime has been installed. FISH073 remains the next implementation task and awaits explicit implementation go-ahead.

Preserve all current source facts, Markdown, images, and actual relationships. Resolve FISH078–079 before full migration acceptance. The user need not approve every migration row, but every discrepancy must be resolved or explicitly accepted. Ordinary releases need no separate staging URL; the initial v2 cutover uses an isolated preview. After verified cutover remove obsolete v1 active code while retaining Git history and a rollback artifact. No permanent legacy adapter or parallel authoritative database.

## Project process

Chat mode is default; Work only for a specific Work-only capability with explanation and prior approval. An authorized change continues through implementation, validation, PR/CI, merge, actual deployment verification and authoritative-record reconciliation without repeated status/approval stops. Restore current main, preserve direct edits, respect denied permissions, keep meaningful tests and never rerun a one-time migration. Historical release records and prior Context/Decision/TODO versions remain in Git and `History/`; they are not competing current authority. The canonical TODO records open work. The v1-specific PWA README remains an accurate implementation reference until cutover.
