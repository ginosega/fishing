# Fishing Companion v2 — Implementation and Acceptance Addendum

**Date:** September 9, 2026. The original Technical Contracts remain the approved design and are preserved unchanged. This addendum records implementation evidence and unresolved engineering work; it does not revise the accepted requirements.

The implementation branch `feature/v2-implementation-20260908` was observed at `24f57ca7d72a9751e0935c6eb46e842f61493db2`; the verified migration was committed at `6615ae7296e48d90dceab303b6b5a1fbc041ab80`. Current source, tests, and the [Work Handoff](Fishing_v2_Work_Handoff_2026-09-09.md) govern the continuation. The implementation contains the Node 24 lockfile, ESM modules, schema/semantic validation, source-aware Gear/KB handoffs, original-image validation, Markdown sanitization, deterministic builder, complete-release service worker, and Playwright suite. The hosted run `34330339245` passed 15 core tests, dependency audit and build verification, but browser acceptance failed four of six scenarios.

Required repairs include accessible viewer controls and precise test selectors; activation and selection of a newly verified release; retry after failed article loading; and preservation of the last-known-good release through failed, partial or corrupt updates. Ensure pointer, manifest, code and content share the same release identity, immutable older releases remain usable by pinned tabs, and bytes are verified before serving or repair. Never delete the only complete cache before a replacement is committed. Retain dirty-form protection, storage best-effort semantics and isolated service-worker scope. P1 authoring remains a handoff, not direct Save.

The migration report incorrectly marks the missing Rapala F-3 picture optional. Correct it to seven required exceptions and one optional generic inline-spinner picture without rewriting approved content. No automatic image acquisition or unapproved replacement is permitted.

The release path must include source validation, deterministic build, real-browser/offline tests, safe combined preview deployment and actual hosted verification. Temporary migration/workspace/acceptance workflows are scaffolding to consolidate into the normal release pipeline without dropping meaningful gates. Preview publication must preserve the v1 root. Explicit preview acceptance and separate production-cutover approval are required. No P2 infrastructure is authorized by this temporary Work session.
