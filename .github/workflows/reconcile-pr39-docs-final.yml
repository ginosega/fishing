name: Final PR39 docs consistency cleanup

on:
  push:
    branches:
      - docs/reconcile-pr39-authored-notes-20260904

permissions:
  contents: write

jobs:
  reconcile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply final docs consistency cleanup if present
        run: |
          if [ -f reconcile-pr39-docs-final.py ]; then
            python reconcile-pr39-docs-final.py
          fi
      - name: Commit final docs consistency cleanup
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if ! git diff --cached --quiet; then
            git commit -m "Finish PR #39 documentation consistency cleanup"
            git push origin HEAD:docs/reconcile-pr39-authored-notes-20260904
          fi
