import fs from 'node:fs/promises';
import assert from 'node:assert/strict';
const edit = async (file, transform) => fs.writeFile(file,transform(await fs.readFile(file,'utf8')));
const rep = (source,before,after) => {assert.ok(source.includes(before),`Missing expected source: ${before.slice(0,100)}`);return source.replace(before,after);};
const version='2026-09-06-my-gear-v4-ordered-links-1';
const release='PR #46';

await edit('pwa/gear-app.js',source => {
  const start=source.indexOf('const ACCESSORIES_ICON = `');
  const end=source.indexOf('\nconst CATEGORY_META = {',start);
  assert.ok(start>=0&&end>start);
  const icon=`const ACCESSORIES_ICON = \`<svg viewBox="0 0 64 64" role="img" aria-label="Kayak">
  <defs><linearGradient id="kayakHull" x1="0" y1="0" x2="1" y2="1"><stop stop-color="#a2def7"/><stop offset="1" stop-color="#76c8ef"/></linearGradient></defs>
  <g transform="rotate(-35 32 32)" stroke-linecap="round" stroke-linejoin="round">
    <path d="M32 3 C40 9 43 20 43 32 C43 46 38 56 32 61 C26 56 21 46 21 32 C21 20 24 9 32 3Z" fill="url(#kayakHull)" stroke="#2f8fc0" stroke-width="2"/>
    <path d="M32 5 C27 12 25 20 25 32 M32 5 C37 12 39 20 39 32" fill="none" stroke="#e5f6fd" stroke-width=".8" opacity=".8"/>
    <path d="M25 15 39 19 M25 19 39 15 M24 45 40 49 M24 49 40 45" fill="none" stroke="#367da3" stroke-width="1.2"/>
    <ellipse cx="32" cy="32" rx="8.5" ry="12" fill="#233b4e" stroke="#377fa4" stroke-width="1.6"/>
    <ellipse cx="32" cy="32" rx="5.2" ry="7.5" fill="#344d60"/>
    <path d="M27 27 Q32 24 37 27 M27 37 Q32 40 37 37" fill="none" stroke="#6d8998" stroke-width="1.2"/>
    <path d="M8 32H56" stroke="#555e63" stroke-width="2.8"/>
    <path d="M8 29 3 25 Q1 32 3 39 L8 35Z" fill="#a9afb2" stroke="#737b80" stroke-width="1.2"/>
    <path d="M56 29 61 25 Q63 32 61 39 L56 35Z" fill="#a9afb2" stroke="#737b80" stroke-width="1.2"/>
  </g>
</svg>\`;`;
  source=source.slice(0,start)+icon+source.slice(end);
  source=rep(source,"          <label class=\"field-label\" for=\"gearNotes\">Notes Markdown</label>\n          <textarea", "          <label class=\"field-label\" for=\"gearNotes\">Notes Markdown</label>\n          <p class=\"form-help\">Upload supporting images alongside the Markdown in pwa/gear-content/. Prefix each filename with the Gear ID, then reference it by filename, for example ![Bow hatch](bonafide-rvr119-bow-hatch.png). New images appear in Preview after they are available on the site.</p>\n          <textarea");
  return source;
});

await edit('pwa/README.md',source => {
  source=source.replaceAll('strict schema-v3 validation','strict schema-v4 validation').replaceAll('schema-v3 validation/display helpers','schema-v4 validation/display helpers');
  source=source.replaceAll('schema version `3`','schema version `4`').replaceAll('`2026-09-04-my-gear-v3-external-notes-1`',`\`${version}\``).replaceAll('**63 current records**','**64 current records**');
  source=rep(source,'Rods & Reels, Line, Weights, Snaps & Swivels, Hooks, Lures, Bait, Accessories','Rods & Reels, Line, Weights, Snaps & Swivels, Hooks, Lures, Bait, Equipment');
  source=source.replaceAll('Accessories Types: Kayaks, Tools, Tackle Management, Electronics, Storage, Miscellaneous','Equipment Types: Kayaks, Tools, Tackle Management, Electronics, Storage, Accessories');
  source=rep(source,'The Accessories category uses the user-approved light-blue kayak / gray-paddle icon. Its current canonical Types are Kayaks, Tools, Tackle Management, Electronics, Storage, and Miscellaneous.','The Equipment category (stable key `accessories`) uses the user-approved light-blue kayak / gray-paddle icon. Its canonical Types are Kayaks, Tools, Tackle Management, Electronics, Storage, and Accessories.');
  source=rep(source,'- optional Manufacturer, Manufacturer URL, Model;','- optional Manufacturer and Model;');
  source=rep(source,'- repeatable typed Link Text/URL rows;','- repeatable ordered Link Text/URL rows, without classifications;');
  source=rep(source,'- Picture filename/path preparation;','- Picture filename/path preparation, actual source display, and explicit Keep/Replace for existing pictures;');
  source=rep(source,'`DATA_MODEL_RECONCILIATION_DESIGN.md` — shared architectural principles, current My Gear schema-v3 rationale, and authored-Notes ownership rules','`DATA_MODEL_RECONCILIATION_DESIGN.md` — shared architectural principles, historical schema-v3 rationale, and authored-Notes ownership rules');
  source += `\n## ${release} — Gear taxonomy, ordered links, and media authoring\n\nMy Gear schema v4 keeps stable IDs and the internal category key \`accessories\` while displaying **Equipment**. The KB's equipment entity type is unchanged, but its card is **Gear Guides**. The six Equipment Types end with Accessories, not Miscellaneous. All 64 existing records are preserved. Manufacturer URLs were migrated to the first ordered Link entry and all link classifications were removed. New and existing product forms use only Link Text and URL; leaf pages show all links in stored order and omit the Links section when empty. Schema-v3 non-seed local stores are upgraded without discarding owned records.\n\n### Authored Notes images\n\nPlace supporting images next to their Markdown in \`pwa/gear-content/\`. Use lowercase safe filenames prefixed by the stable Gear ID, such as \`bonafide-rvr119-bow-hatch.png\`. Reference the image from \`bonafide-rvr119.md\` as \`![Bow hatch](bonafide-rvr119-bow-hatch.png)\`. The build validates referenced local images as JPEG, PNG, WebP, or GIF, checks actual format against extension, requires nonempty files no larger than 10 MiB, and rejects missing, unsafe, or unowned paths. Existing \`assets/gear-notes/\` references remain supported. Exact image bytes are copied without recompression, and the generated Notes manifest includes each referenced image for offline caching. The main Gear hero/thumbnail picture remains a separate media-owned asset.\n\nNotes Markdown can be edited directly on main; GitHub Actions still rebuilds and deploys on commits. Images must be uploaded directly to GitHub, not transported through ChatGPT. Preview resolves already-deployed relative images; newly uploaded images become visible after their deployment.\n\n### Existing picture replacement\n\nEdit displays the current deployed asset, stable media ID, and actual remote source URL or repository source path. With Picture Yes, choose **Keep current picture** (default) or **Replace picture**. Replace permits the same filename and generates a source upload path under \`pwa/assets/gear-source/\`. The handoff includes the existing media ID, asset/source, and requested replacement path. After the user uploads the binary directly to GitHub, repository promotion preserves the original media ID/owner and registers the new source in \`local-media.json\`; no duplicate media identity is created. The old remote source remains available as provenance. Existing images are not bulk-migrated or deleted.\n\nFor Cylinder Weights, retain Gear ID \`cylinder-weights\` and media ID \`thkfish-cylinder-weights\`. A replacement may use \`pwa/assets/gear-source/cylinder-weights.png\` or the matching actual extension. No replacement has been received or registered yet.\n`;
  return source;
});

await edit('Fishing_Context.md',source => {
  source=rep(source,'- schema version `3`\n- data version `2026-09-04-my-gear-v3-external-notes-1`\n- **63 records**','- schema version `4`\n- data version `2026-09-06-my-gear-v4-ordered-links-1`\n- **64 records**');
  source=rep(source,'allowed categories: Rods & Reels, Line, Weights, Snaps & Swivels, Hooks, Lures, Bait, Accessories','allowed categories: Rods & Reels, Line, Weights, Snaps & Swivels, Hooks, Lures, Bait, Equipment (stable key `accessories`)');
  source=rep(source,'current Accessories Types: Kayaks, Tools, Tackle Management, Electronics, Storage, Miscellaneous','current Equipment Types: Kayaks, Tools, Tackle Management, Electronics, Storage, Accessories');
  source=rep(source,'PR #42 added browser Add/Edit **authoring and handoff**','PR #42 added browser Add/Edit **authoring and handoff**');
  source += `\n## 2026-09-06 Gear authoring refinement\n\nThe current schema-v4 Gear release preserves 64 records and all stable IDs. My Gear displays Equipment (internal key \`accessories\`); the KB equipment card displays Gear Guides. Links are ordered Label/URL pairs, with legacy manufacturer URLs migrated into the first link and no classifications. Both existing and new forms use the simplified fields. The approved light-blue kayak/gray-paddle icon remains the Equipment icon.\n\nSupporting Gear Notes images may live beside the stable-ID Markdown in \`pwa/gear-content/\`, with safe Gear-ID-prefixed filenames and relative references. The build validates image format/size/path and adds referenced assets to offline manifests; old \`assets/gear-notes/\` references remain valid. Direct GitHub binary upload is still required.\n\nExisting-picture editing displays the actual source and media identity and offers explicit Keep/Replace. Same-filename replacement is supported; a replacement handoff retains the original media ID/owner and identifies the exact \`pwa/assets/gear-source/\` upload path. Legacy remote images are promoted individually as users replace them, not bulk-migrated. Cylinder Weights still uses media ID \`thkfish-cylinder-weights\`; its replacement binary is pending.\n`;
  return source;
});

await edit('Fishing_TODO.md',source => {
  assert.ok(!source.includes('FISH-TODO-058'));
  const row='| FISH-TODO-058 | 2026-09-06 | PWA / Gear authoring | Refined My Gear Equipment and KB Gear Guides naming, migrated Gear to schema v4 ordered links, enabled Gear-ID-prefixed sibling Notes images with offline validation, and added explicit legacy-picture Keep/Replace handoff preserving media identity. Existing 64 records and stable IDs retained; Cylinder Weights replacement remains awaiting the user-supplied binary. |';
  source=rep(source,'## Completed Items\n','## Completed Items\n\n'+row+'\n');
  return source;
});

await edit('Fishing_Decision_Log.md',source => source+`\n## 2026-09-06 — Gear taxonomy, links, and media authoring\n\n- My Gear category **Equipment** retains the internal \`accessories\` key; KB equipment is displayed as **Gear Guides** with subtitle \`Equipment, rigs, and presentations reference\`. Equipment Types are Kayaks, Tools, Tackle Management, Electronics, Storage, Accessories. Taxonomy remains chat-managed.\n- Gear schema v4 stores ordered \`{label,url}\` links only. Legacy manufacturer URLs become the first link and classifications are removed. The form has no separate Manufacturer URL or Link Type field. Empty Links sections remain hidden. Existing IDs and facts are preserved.\n- Gear Notes images may be colocated with Markdown in \`pwa/gear-content/\` using Gear-ID-prefixed filenames and relative references. Validate source image bytes, extension, size, ownership/path, and offline inclusion. Preserve existing legacy Notes image paths. Hero media remains separate.\n- Existing Gear pictures expose source/identity and explicit Keep/Replace. Same-filename replacement is allowed. A replacement uses direct GitHub upload into \`pwa/assets/gear-source/\` and retains the old media ID/owner. Convert legacy images as requested rather than bulk-migrating. Do not invent a replacement or delete current media before a valid uploaded source is registered.\n- Browser authoring remains handoff-only; no direct GitHub write or divergent local database.\n`);

await edit('README.md',source => source+`\n### Current Gear authoring refinement\n\nMy Gear now uses schema v4 with ordered, unclassified links and the user-facing Equipment category (stable key \`accessories\`); the KB card is Gear Guides. Gear Notes support validated, Gear-ID-prefixed sibling images in \`pwa/gear-content/\`. Existing image editing distinguishes Keep from Replace, preserves media identity, and supplies an exact repository upload path. See \`pwa/README.md\` for the current policy and implementation.\n`);
await edit('Fishing_New_Chat_Bootstrap_Prompt.md',source => source+`\n## Current Gear authoring release (2026-09-06)\n\nRestore the latest main before acting. Current My Gear schema is v4, dataVersion \`${version}\`, with 64 records at release. Equipment is the My Gear category label (internal key \`accessories\`), with Types Kayaks, Tools, Tackle Management, Electronics, Storage, Accessories. The KB equipment card is Gear Guides. Links are ordered Label/URL pairs without classifications or a separate manufacturer URL. Preserve stable IDs and all existing facts.\n\nNotes images may be colocated in \`pwa/gear-content/\`, named with the owning Gear-ID prefix and referenced relatively. The build validates local image format/size/path and includes referenced images offline; legacy \`assets/gear-notes/\` paths continue to work. User image binaries must be uploaded directly to GitHub. Existing-picture edits must distinguish Keep/Replace, display the actual source, support same-filename replacement, and preserve media ID/owner when promoting a new local source. Cylinder Weights retains media ID \`thkfish-cylinder-weights\`; no replacement binary has been supplied yet.\n\nThis current-state section supersedes any older schema-v3, Accessories/Miscellaneous, typed-links, or generated-image-filename instructions elsewhere in this bootstrap. Chat mode remains the default; never recommend Work merely for complexity, duration, files, analysis, or artifacts.\n`);
console.log('Release icon and durable-state documentation reconciled.');
